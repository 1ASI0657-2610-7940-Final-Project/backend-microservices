name: chat-notification-service

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'services/chat-notification-service/**'
      - '.github/workflows/chat-notification-service.yml'
  push:
    branches: [ feature/chat-notification-service ]
    paths:
      - 'services/chat-notification-service/**'
      - '.github/workflows/chat-notification-service.yml'

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/chat-notification-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - run: mvn clean verify
